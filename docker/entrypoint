#!/bin/bash
#----------------------------------------------------------------------------------
#
#   Проект: Молот [molot]
# 
#   Файл служит точкой входа при создании и запуске контейнера
#   для сборки пакета Квас Про в среде разработке под Entware
# 
#   Запускается ВНУТРИ контейнера 
# 
#   Автор       : Железа
#   Email       : dev@zeleza.ru
#   Лицензия    : Apache Версия 2.0
#
#   Создан      : 27 октября 2024 год
#   Обновлен    : 30 октября 2024 год
#
#----------------------------------------------------------------------------------


# Включам режим вывода отладочных сообщений
# set -x

# Подключаем библиотеку общих функций
source ${HOME}/${APP_NAME}/molot/libs/general
source ${HOME}/${APP_NAME}/molot/libs/container

# Удаляем файл с индикатором сборки
[ -f ${BUILDED_STAGES_FILE} ] && rm -f ${BUILDED_STAGES_FILE}

# Устанавливаем bash-профиль для вывода вспоможностей
set_bashrc_profile 

# Запускаем интерактивную сессию
bash 

# Выход должен быть из ENTRYPOINT 
# обязательно успешным, т.е. 0
exit 0